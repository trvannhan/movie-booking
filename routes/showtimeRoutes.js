// ============================================
// routes/showtimeRoutes.js — Routes lịch chiếu (sẽ làm Sprint sau)
// ============================================
const express = require('express');
const router = express.Router();

router.get('/', (req, res) => {
  res.send('Trang lịch chiếu - đang phát triển');
});

module.exports = router;
